package Diccionarios;
public class ConceptoDiccionarios {
    public static void main(String[] args) {
        /*Un diccionario en Java contiene una serie de elementos que trabajan como entrada
        estas estan formadas por un par (clave, valor).
        La clave como key permite acceder al valor, las claves son unicas
        En Java existen varios tipos de disccionario:
        
        * HashMap
        Metodos:
        -get(Clave) Obtiene el valor que se encuentra en la clave.
        -put(Clave, valor) Agrega el valor a la clave (Si existe un valor en esta la sobreescribe)
        -KeySet() Retorna un grupo de valores.
        -values() Retorna todos los valores.
        -entrySet() Retorna un conjunto de todos los pares.
        -containsKey(clave) Retorna V O F si encuentra la clave.
        -getKey() Retorna la clave de un valor.
        -getValue() Retorna el valor de la clave.
        * Enummap
        * HashTable
        */
    }
}
